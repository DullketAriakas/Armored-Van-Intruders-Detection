[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "Practica_final"
version = "0.1"
requires-python = ">=3.9"
dependencies = [
    "requests",
    "cryptography",
    "flask",
    "pycryptodome"
]
 
